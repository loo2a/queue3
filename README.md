# نظام نداء المرضى - Clinic Queue System

نظام متكامل لإدارة طوابير المرضى في المراكز الطبية باللغة العربية

## 📁 هيكل المشروع على GitHub

```
clinic-queue/
├── index.html              # صفحة الإدارة
├── display.html            # شاشة العرض
├── control.html            # صفحة التحكم
├── print.html              # صفحة طباعة التذاكر
├── audio/                  # مجلد الصوتيات
│   ├── ding.mp3
│   ├── prefix.mp3          # "على العميل رقم"
│   ├── and.mp3             # "و"
│   ├── 0.mp3 إلى 9.mp3    # الأحاد
│   ├── 10.mp3 إلى 90.mp3  # العشرات
│   ├── 100.mp3 إلى 900.mp3 # المئات
│   └── clinic1.mp3 إلى clinicN.mp3
├── media/                  # مجلد الفيديوهات
│   ├── health1.mp4
│   ├── health2.mp4
│   └── ...
├── js/                     # ملفات JavaScript
│   ├── audio-handler.js    # معالج الصوتيات
│   ├── firebase-config.js  # إعدادات Firebase
│   └── queue-manager.js    # مدير الطابور
└── instant/                # صوتيات جاهزة
    ├── announcement1.mp3
    ├── announcement2.mp3
    └── emergency.mp3
```

## 🔧 خطوات التثبيت

### 1. إنشاء Repository على GitHub

```bash
# إنشاء مجلد المشروع
mkdir clinic-queue
cd clinic-queue

# تهيئة Git
git init
git branch -M main

# إنشاء المجلدات
mkdir audio media js instant

# إضافة الملفات
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/clinic-queue.git
git push -u origin main
```

### 2. تفعيل GitHub Pages

1. اذهب إلى Settings في الـ Repository
2. انتقل إلى Pages
3. اختر Branch: main
4. احفظ التغييرات
5. الموقع سيكون متاح على: `https://YOUR_USERNAME.github.io/clinic-queue/`

### 3. إعداد Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. أنشئ مشروع جديد
3. فعّل Realtime Database
4. اضبط القواعد:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

5. انسخ بيانات المشروع وضعها في `js/firebase-config.js`

## 🎵 ملفات الصوت المطلوبة

### في مجلد `audio/`:

**الأحاد (0-9):**
- 0.mp3, 1.mp3, 2.mp3, ... 9.mp3

**العشرات (10-90):**
- 10.mp3, 20.mp3, 30.mp3, ... 90.mp3

**المئات (100-900):**
- 100.mp3, 200.mp3, 300.mp3, ... 900.mp3

**ملفات خاصة:**
- prefix.mp3: "على العميل رقم"
- and.mp3: "و"
- ding.mp3: صوت تنبيه

**ملفات العيادات:**
- clinic1.mp3: "عيادة طب الأسرة"
- clinic2.mp3: "عيادة الأسنان"
- clinic3.mp3: "عيادة الجلدية"
- ... (حسب عدد العيادات)

## 🎯 كيفية عمل نظام النداء

### مثال: نداء رقم 468 في عيادة طب الأسرة

**التسلسل:**
1. ding.mp3 (صوت تنبيه)
2. prefix.mp3 ("على العميل رقم")
3. 400.mp3 ("أربعمائة")
4. and.mp3 ("و")
5. 60.mp3 ("ستون")
6. and.mp3 ("و")
7. 8.mp3 ("ثمانية")
8. clinic1.mp3 ("عيادة طب الأسرة")

### مثال: نداء رقم 25 في عيادة الأسنان

**التسلسل:**
1. ding.mp3
2. prefix.mp3
3. 20.mp3 ("عشرون")
4. and.mp3
5. 5.mp3 ("خمسة")
6. clinic2.mp3 ("عيادة الأسنان")

## 💾 استخدام التخزين

النظام يستخدم **Persistent Storage** لحفظ البيانات:

```javascript
// حفظ البيانات
await window.storage.set('clinic-settings', JSON.stringify(settings));

// قراءة البيانات
const result = await window.storage.get('clinic-settings');
const settings = JSON.parse(result.value);

// البيانات المشتركة (للعرض)
await window.storage.set('current-call', JSON.stringify(callData), true);
```

## 📱 الصفحات الأربعة

### 1. صفحة الإدارة (index.html)
- تعديل بيانات المركز
- إدارة العيادات
- نداء مخصص
- عرض أسماء
- تشغيل ملفات صوتية

### 2. شاشة العرض (display.html)
- عرض العيادات والأرقام
- فيديوهات التوعية
- شريط الأخبار المتحرك
- QR Code
- تنبيهات النداء

### 3. صفحة التحكم (control.html)
- العميل التالي/السابق
- تكرار النداء
- نداء مخصص
- إيقاف/استئناف العيادة

### 4. صفحة الطباعة (print.html)
- طباعة تذاكر جماعية
- تنسيق A4
- مربعات 5×5 سم

## 🎨 المميزات

✅ واجهة عربية كاملة (RTL)
✅ نظام نداء ذكي بتركيب الأرقام
✅ احتياطي TTS عند فشل الصوت
✅ تخزين دائم للبيانات
✅ تحديثات فورية عبر Firebase
✅ تصميم متجاوب (Tailwind CSS)
✅ مجاني 100%

## 🔊 إنشاء ملفات الصوت

يمكنك استخدام:
- **Google Text-to-Speech**
- **Microsoft Azure TTS**
- **تسجيل صوتي احترافي**

أو استخدام Python لإنشاء الملفات:

```python
from gtts import gTTS
import os

numbers = {
    0: 'صفر', 1: 'واحد', 2: 'اثنان', 3: 'ثلاثة',
    4: 'أربعة', 5: 'خمسة', 6: 'ستة', 7: 'سبعة',
    8: 'ثمانية', 9: 'تسعة'
}

for num, text in numbers.items():
    tts = gTTS(text=text, lang='ar')
    tts.save(f'audio/{num}.mp3')
```

## 📞 الدعم

للأسئلة أو المساعدة، يمكنك فتح Issue على GitHub

## 📄 الترخيص

هذا المشروع مجاني ومفتوح المصدر - استخدمه كما تشاء!

---

**تم التطوير بواسطة Claude** 🤖
